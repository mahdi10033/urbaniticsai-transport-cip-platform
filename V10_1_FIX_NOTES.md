# v10.1 Fix

Fixed an indentation error in the sidebar weighting/filter block.

The app now keeps:
- percentage-based weights
- total weight must equal 100%
- scenario and filter controls
- budget controls
